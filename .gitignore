# macOS
.DS_Store

# Картинки с координатной сеткой — рабочий инструмент агента-аннотатора и
# агента-корректора координат. В git не нужны: восстанавливаются командой
# python3 scripts/make_grid_images.py <content_dir> (репозиторий redpen-infra),
# а весят сотни мегабайт (11 класс — 212 МБ, 10 класс — 223 МБ).
*/images_with_grid/
